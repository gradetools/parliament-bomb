<html>

<head>
    <title>Acknowledgments -- ParliamentBomb</title>
    <style>
        @media (prefers-color-scheme: dark) {
            body {
                background: #333;
                color: white;
            }
            a {
                color: skyblue;
            }
        }
        .container {
            font-family: sans-serif;
            max-width: 800px;
            margin: 0 auto;
        }
        .intro {
            text-align: center;
        }
        .licenses-list {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }
        .licenses-list {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }
        .license-used-by {
            margin-top: -10px;
        }
        /* Removed max-height and overflow-y properties to allow the license text to expand as needed */
        .license-text {
            white-space: pre-wrap;
        }
    </style>
</head>

<body>
    <main class="container">
        <div class="intro">
            <h1>Acknowledgements & Third Party Licenses</h1>
            <p>This page lists the licenses of the open source libraries used in ParliamentBomb.</p>
            <p>ParliamentBomb is Copyright (c) 2024, gradetools contributors, under the BSD-3-Clause license.<p>
        </div>
        <hr class="rounded">
        <ul class="licenses-list">
            {{#each licenses}}
            <li class="license">
                <h3>{{name}}</h3>
                <h4>Used by:</h4>
                <ul class="license-used-by">
                    {{#each used_by}}
                    <li><a href="{{#if crate.repository}} {{crate.repository}} {{else}} https://crates.io/crates/{{crate.name}} {{/if}}">{{crate.name}} {{crate.version}}</a></li>
                    {{/each}}
                </ul>
                <pre class="license-text">{{text}}</pre>
            </li>
            {{/each}}
        </ul>
    </main>
</body>

</html>

